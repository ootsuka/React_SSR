import React from 'react'

const home = () => {
  return(
    <div>
      <div>home sweet home!</div>
      <button onClick={() => alert('clicked')}>
        click
      </button>
    </div>

  )
}

export default home
